<!DOCTYPE html>
<html>
<head>
  <title>Cities</title>
</head>
<body>
  <h1>Search Cities</h1>
  <%= form_tag cities_path, method: :get do %>
    <label for="state_id">State:</label>
    <%= select_tag :state_id, options_from_collection_for_select(State.all, :id, :name, params[:state_id]), include_blank: 'Select a state' %>
    
    <label for="name">City Name:</label>
    <%= text_field_tag :name, params[:name] %>
    
    <%= submit_tag 'Search' %>
  <% end %>
  <ul>
    <% @cities.each do |city| %>
      <li><%= city.name %> (<%= city.state.uf %>) - População: <%= number_with_delimiter(city.population, delimiter: '.') %> </li>
    <% end %>
  </ul>
</body>
</html>
